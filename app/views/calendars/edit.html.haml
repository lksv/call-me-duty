.page-header
  = link_to team_calendars_path(@team), class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to @calendar, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-info-sign
    Show
  %h1 Editing calendar

= render 'form'
